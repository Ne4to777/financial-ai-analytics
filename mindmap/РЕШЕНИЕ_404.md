# ✅ Решение проблемы 404 с прототипами

## 🔍 Проблема

В консоли браузера:
```
:8000/prototypes/index.html:1 Failed to load resource: 404
01-landing.html:1 Failed to load resource: 404
```

## 🎯 Причина

**Сервер запускался из неправильной директории!**

```bash
❌ Запуск из viewer/:
cd /Users/nybble/projects/ident/mindmap/viewer
live-server --port=8000

→ Сервер ищет: viewer/prototypes/ (не существует!)

✅ Правильный запуск из mindmap/:
cd /Users/nybble/projects/ident/mindmap
live-server --port=8000

→ Сервер находит: mindmap/prototypes/ ✓
```

---

## 🚀 Решение

### Используй скрипт запуска:

```bash
cd /Users/nybble/projects/ident/mindmap
./start-server.sh
```

Этот скрипт **гарантирует**, что сервер запустится из правильной директории!

---

## ✅ Теперь работает:

1. ✅ http://localhost:8000/viewer/view-diagram-full.html
2. ✅ http://localhost:8000/prototypes/index.html
3. ✅ http://localhost:8000/prototypes/01-landing.html
4. ✅ Iframe в viewer'е загружает прототипы без 404
5. ✅ Кнопки навигации переключают между прототипами

---

## 📝 Важно

**Всегда запускай сервер из папки `mindmap/`**, а не из `viewer/` или других вложенных папок!

**Структура должна быть:**
```
mindmap/               ← ЗАПУСКАЙ ОТСЮДА!
├── viewer/
│   └── view-diagram-full.html
├── prototypes/
│   ├── index.html
│   ├── 01-landing.html
│   ├── 02-upload.html
│   └── 03-analysis.html
└── start-server.sh
```

---

## 🧪 Проверка

После запуска сервера, проверь в браузере:

1. Открой http://localhost:8000/prototypes/index.html напрямую
2. Если видишь навигационную страницу (без 404) → ✅ Всё ОК!
3. Теперь открой viewer с прототипами: http://localhost:8000/viewer/view-diagram-full.html?diagram=prototypes
4. Должен увидеть фиолетовую панель с кнопками и iframe с прототипами

---

**Дата:** 2026-01-25  
**Статус:** ✅ Исправлено
