# Changelog

## 2026-01-25 - Исправление просмотра прототипов + Отдельные пункты меню

### ✨ Новое
- **Отдельные пункты меню для каждого прототипа!** Теперь можно открыть любой прототип напрямую из бокового меню:
  - 📋 Навигация по прототипам
  - 🏠 Landing Page
  - 📤 Upload Screen
  - 📊 Analysis Results
- Прямые ссылки для каждого прототипа (например, `?diagram=prototype-landing`)
- Автоматическое выделение активного пункта в меню

### Исправлено ✅
- **Прототипы теперь отображаются правильно!** Добавлена поддержка `/prototypes/` путей в `live-server.js`
- Исправлен доступ к HTML прототипам через viewer
- Добавлено наблюдение за директорией prototypes для hot reload

### Изменения в `viewer/live-server.js`:
1. Добавлена обработка `/prototypes/*` запросов (редирект на `../prototypes/`)
2. Добавлена директория `../prototypes` в список отслеживаемых для hot reload

### Как использовать:
1. Запустите сервер:
   ```bash
   cd mindmap/viewer
   node live-server.js
   ```

2. Откройте в браузере:
   - Viewer: http://localhost:8000/view-diagram-full.html
   - Прототипы напрямую: http://localhost:8000/prototypes/
   - Или через viewer: выберите "📱 HTML Прототипы экранов MVP" в боковом меню

3. Все прототипы доступны:
   - ✅ Landing Page (01-landing.html)
   - ✅ Upload Screen (02-upload.html)
   - ✅ Analysis Results (03-analysis.html)

### Проверка работоспособности:
```bash
# Все должны вернуть 200
curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/prototypes/index.html
curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/prototypes/01-landing.html
curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/prototypes/02-upload.html
curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/prototypes/03-analysis.html
```

### Технические детали:
- Сервер запускается из директории `viewer/`
- Обслуживает файлы из `viewer/`, `../diagrams/`, и `../prototypes/`
- Поддержка hot reload для всех директорий
- WebSocket на порту 8001 для live reload
- HTTP сервер на порту 8000
