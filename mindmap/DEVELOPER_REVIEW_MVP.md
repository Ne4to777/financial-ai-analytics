# 🔍 Developer Review: MVP Architecture

## Вопрос: Взялся бы я за реализацию этого MVP?

### ✅ **ДА, но с условиями и доработками**

---

## 🎯 Что ХОРОШО (можно брать в работу)

### 1. **Четкий scope и ограничения**
✅ Понятно, ЧТО делаем и ЧТО НЕ делаем
- Stateless approach
- Один endpoint
- Анонимный доступ
- Web-only

**Оценка:** Отлично. Scope четкий, можно оценить.

### 2. **Простая архитектура**
✅ Монолит с одним API endpoint
- Express.js (стандартный стек)
- Известные библиотеки (Multer, PapaParse)
- Минимум зависимостей

**Оценка:** Хорошо. Нет overengineering.

### 3. **Реалистичные ограничения**
✅ Max 10MB CSV, 30-60s response time
- Не обещаем невозможного
- Четкие метрики производительности

**Оценка:** Отлично. Реалистично.

### 4. **Понятный deployment**
✅ Railway/Render + Vercel + Supabase
- Все managed сервисы
- Простой setup
- ~$10/месяц

**Оценка:** Отлично для MVP.

---

## ⚠️ Что ВЫЗЫВАЕТ ВОПРОСЫ (требует доработки)

### 1. **🚨 КРИТИЧНО: AI Промпт недостаточно детален**

**Проблема:**
```
Промпт (как в диаграмме 8 - ПОЛНАЯ мощь):
"Analyze this financial CSV data..."
```

❌ Это placeholder, не реальный промпт!

**Что отсутствует:**
- Точный формат входных данных для LLM
- System message vs User message
- Temperature, max_tokens, top_p
- Примеры (few-shot learning)
- Fallback стратегия при ошибках LLM
- Structured output (JSON mode в GPT-4?)

**Риск:** Без точного промпта получим непредсказуемые результаты.

**Рекомендация:**
```typescript
// НУЖЕН ТОЧНЫЙ ПРОМПТ:
const SYSTEM_MESSAGE = `You are a financial analyst...`;
const USER_PROMPT_TEMPLATE = `Analyze this CSV with ${rows.length} transactions...`;
const CONFIG = {
  model: 'gpt-4o-mini',
  temperature: 0.3,  // Для consistency
  max_tokens: 2000,
  response_format: { type: "json_object" }  // Structured output
};
```

**Трудозатраты:** +2-3 дня на тестирование промпта с реальными CSV.

---

### 2. **🚨 КРИТИЧНО: Формат CSV не определен**

**Проблема:**
```
Ожидаемые колонки: date, category, amount, description
```

❌ А если колонки называются по-другому?
- `Date` vs `date` vs `Дата` vs `Transaction Date`?
- `Amount` vs `Сумма` vs `Value` vs `Total`?
- Разные валюты? (USD vs RUB vs EUR)
- Разные разделители? (`,` vs `;`)
- Encoding? (UTF-8 vs Windows-1251)

**Реальность:** Пользователи загрузят CSV из 1C, Excel, QuickBooks - все будут разные!

**Рекомендация:**
1. **Либо:** Строгий формат + шаблон для скачивания + валидация
2. **Либо:** Умный парсер с автодетектом колонок (ML/эвристики)

**Трудозатраты:**
- Вариант 1 (строгий формат): +1 день
- Вариант 2 (умный парсер): +5-7 дней

**Мой выбор:** Вариант 1 для MVP, Вариант 2 для Tier 2.

---

### 3. **⚠️ ВАЖНО: Обработка ошибок не расписана**

**Что может пойти не так:**
- CSV некорректный (битый файл, неправильные колонки)
- LLM API недоступен (timeout, rate limit, 500 error)
- LLM вернул невалидный JSON
- PostgreSQL недоступен (сохранение логов)
- Out of memory (большой CSV)
- Timeout на клиенте (> 60s)

**Что делаем в каждом случае?**

**Рекомендация:**
```typescript
// Error handling strategy:
try {
  const analysis = await analyzeWithAI(metrics);
} catch (error) {
  if (error.code === 'TIMEOUT') {
    return { metrics, risks: [], message: "AI анализ недоступен" };
  }
  // ... другие сценарии
}
```

**Трудозатраты:** +2 дня на полный error handling.

---

### 4. **⚠️ ВАЖНО: PostgreSQL "optional" - но логика от него зависит**

**Противоречие:**
```
💾 Хранилище (optional)
...
4. logAnalysis() → void
   - Сохранение в PostgreSQL
```

❌ Если optional, то код должен работать БЕЗ БД!

**Вопросы:**
- Что если БД недоступна? Падаем или игнорируем?
- Как тестировать локально без PostgreSQL?
- Нужен ли fallback на file-based logging?

**Рекомендация:**
```typescript
// Make logging truly optional:
async function logAnalysis(data) {
  if (!process.env.DATABASE_URL) {
    console.warn('DB not configured, skipping log');
    return;
  }
  try {
    await db.query(INSERT_LOG_SQL, data);
  } catch (error) {
    console.error('Failed to log, continuing...', error);
    // НЕ бросаем ошибку наверх!
  }
}
```

**Трудозатраты:** +0.5 дня.

---

### 5. **⚠️ Security: Rate limiting по IP недостаточно**

**Проблема:**
```
Rate limiting → IP-based ограничения (10 анализов/день)
```

❌ Легко обойти (VPN, proxy, Tor)

**Риски:**
- Abuse: кто-то запустит 1000 запросов
- Расходы на OpenAI взлетят ($100+/день)
- DDoS: сервер ляжет

**Рекомендация:**
1. **Must-have:** Rate limit по IP (express-rate-limit)
2. **Nice-to-have:** Captcha после 3-го запроса
3. **Must-have:** Max concurrent requests (10-20)
4. **Must-have:** Request timeout (60s)
5. **Must-have:** Cost limit на OpenAI API ($50/день alert)

**Трудозатраты:** +1 день.

---

### 6. **📊 Metrics Calculation: Недостаточно спецификации**

**Проблема:**
```typescript
calculateMetrics(rows) → { metrics: {...} }
// Burn rate: avg monthly costs (last 3 months)
```

❌ Много неясностей:
- Что если данных меньше 3 месяцев?
- Как группировать по месяцам? (calendar month vs 30 days)
- Что если пробелы в данных? (нет транзакций в феврале)
- Как считать YoY если нет данных за прошлый год?
- Как обработать дубли? (одна транзакция дважды)

**Рекомендация:**
Нужна **детальная спецификация** для каждой метрики:
```typescript
/**
 * Calculates burn rate
 * @param rows - Array of transactions, MUST be sorted by date DESC
 * @returns Average monthly costs for last 3 complete months
 * @throws Error if less than 3 months of data available
 */
function calculateBurnRate(rows: Transaction[]): number {
  // ...implementation with all edge cases
}
```

**Трудозатраты:** +3-4 дня на всё (+unit tests!).

---

### 7. **🎨 Frontend: Flutter Web - есть ли опыт?**

**Вопрос:** Кто будет делать Flutter Web?

**Проблемы Flutter Web для MVP:**
- ❌ SEO сложнее (важно для Landing page!)
- ❌ Initial load time больше (большой bundle)
- ❌ Не все библиотеки работают на Web
- ❌ Нужен опыт Flutter + Web специфика

**Альтернатива для MVP:**
- **React/Vue/Svelte:** Быстрее для Web MVP
- **Flutter Web:** Оставить для Tier 2 (когда нужны Mobile apps)

**Если Flutter - риски:**
- Tricky drag & drop на Web
- Chart libraries (нужны Web-совместимые)
- Responsive design сложнее

**Рекомендация:**
1. **Либо:** React/Next.js для MVP (быстрее + лучше SEO)
2. **Либо:** Flutter только если есть опытный Flutter dev

**Трудозатраты:**
- React MVP: 5-7 дней
- Flutter Web MVP: 7-10 дней (если есть опыт)

---

### 8. **💰 Costs: OpenAI может стоить дороже**

**Проблема:**
```
LLM API: ~$1-5/month
```

❌ Слишком оптимистично!

**Реальный расчет:**
```
Допустим: 100 анализов/месяц
Prompt size: 2000 tokens input + 2000 tokens output
Cost per analysis: ~$0.001 (GPT-4o-mini)
Total: 100 * $0.001 = $0.10/месяц ✅

НО! Если промпт 10K tokens + response 5K tokens:
Cost: ~$0.002 * 100 = $0.20/месяц ✅

НО! Если 1000 анализов/месяц (success!):
Cost: ~$2-5/месяц ✅ OK

НО! Если abuse (10K requests):
Cost: ~$20-50/месяц 🚨 Проблема!
```

**Рекомендация:**
- Set hard limit на OpenAI API ($50/месяц)
- Alert при $10 spent
- Fallback: если лимит исчерпан → возврат только metrics (без AI)

**Трудозатраты:** +0.5 дня.

---

## 📝 Недостающие спецификации

### Что нужно ДО старта разработки:

1. **✅ Точный AI промпт** (с примерами input/output)
2. **✅ CSV format specification** (шаблон + примеры файлов)
3. **✅ API contract** (OpenAPI/Swagger spec для `/analyze`)
4. **✅ Error responses** (все коды ошибок + messages)
5. **✅ UI mockups** (хотя бы Figma wireframes для 3 экранов)
6. **✅ Metrics formulas** (точные формулы для всех 15 метрик)
7. **✅ Test CSV files** (5-10 примеров для тестирования)
8. **❓ Environment setup** (как запустить локально?)

**Трудозатраты на спецификации:** 2-3 дня (до кода!)

---

## ⏱️ Реалистичная оценка трудозатрат

### Backend (Node.js + Express + PostgreSQL)
```
✅ Setup (Express, DB, env)           1 день
✅ CSV upload endpoint + validation   1 день
⚠️ CSV parsing + column detection     2 дня (умная логика)
⚠️ Metrics calculation (15+ метрик)   3 дня (+ edge cases + tests)
🚨 AI integration (промпт + retry)    3 дня (итерации на реальных данных)
✅ PostgreSQL logging                 1 день
⚠️ Error handling + validation        2 дня
⚠️ Rate limiting + security           1 день
✅ Deployment (Railway setup)         0.5 дня
✅ Testing + bugfixes                 2 дня

ИТОГО: 16-17 дней (~ 3 недели)
```

### Frontend (Flutter Web или React)
```
✅ Setup + routing                    1 день
✅ Landing page                       1.5 дня
✅ Upload page (drag & drop)          1.5 дня
⚠️ Analysis dashboard (15+ метрик)    4 дня (много UI components!)
⚠️ Charts (graphs, trends)            2 дня
✅ Responsive design                  1 день
✅ Error states + loading             1 день
✅ Deployment (Vercel)                0.5 дня
✅ Testing + bugfixes                 2 дня

ИТОГО: 14-15 дней (~ 3 недели)
```

### Integration + E2E
```
✅ Backend ↔ Frontend integration     2 дня
✅ E2E testing (happy path)           1 день
⚠️ Edge cases testing                 2 дня
✅ Load testing (10-20 concurrent)    1 день
✅ Bugfixes from testing              2 дня

ИТОГО: 8 дней (~ 1.5 недели)
```

---

## 📊 ИТОГОВАЯ ОЦЕНКА

### Минимум (все идеально):
**38-40 дней = ~8 недель = 2 месяца**

### Реалистично (с итерациями):
**50-55 дней = ~11 недель = 2.5-3 месяца**

### С запасом (учитывая unknowns):
**60-70 дней = ~14 недель = 3-3.5 месяца**

**Team size:** 1 fullstack developer (или 2 dev: backend + frontend)

---

## ✅ Мой вердикт: **ВЗЯЛСЯ БЫ, но...**

### Условия для старта:

1. ✅ **Нужны спецификации** (промпт, CSV format, API contract)
2. ✅ **Есть UI mockups** (Figma или хотя бы sketches)
3. ✅ **Есть test data** (5-10 реальных CSV файлов)
4. ✅ **Понятны приоритеты** (что critical, что nice-to-have)
5. ✅ **Бюджет на AI API** ($50-100 на тестирование)

### Риски, которые нужно принять:

1. ⚠️ **AI непредсказуем** - может давать плохие результаты (нужны итерации)
2. ⚠️ **CSV formats разные** - пользователи загрузят всякое (нужна валидация)
3. ⚠️ **Performance может быть хуже** - 60s это долго (но для MVP OK)
4. ⚠️ **Abuse возможен** - rate limiting по IP не панацея

---

## 🎯 Рекомендации перед стартом

### Must-have (блокеры):
1. ✅ Точный AI промпт + тестирование на 10 CSV
2. ✅ CSV format spec + валидация
3. ✅ API contract (OpenAPI)
4. ✅ UI mockups (Figma)

### Should-have (важно):
5. ✅ Error handling strategy
6. ✅ Metrics calculation spec (все формулы)
7. ✅ Cost limits на OpenAI
8. ✅ E2E test plan

### Nice-to-have (можно потом):
9. ⭕ Captcha (добавим если будет abuse)
10. ⭕ Advanced rate limiting
11. ⭕ Analytics (Mixpanel/PostHog)

---

## 🚀 Готов начать?

**ДА**, если:
- ✅ Есть 2.5-3 месяца на разработку
- ✅ Есть бюджет $100-200 на инфраструктуру + тестирование
- ✅ Готовы итерировать на AI промпте (1-2 недели)
- ✅ Есть реальные CSV для тестирования

**НЕТ**, если:
- ❌ Нужно "за 2 недели"
- ❌ Нет примеров CSV файлов
- ❌ Нет спецификации промпта
- ❌ Нет UI mockups

---

## 💡 Альтернативные подходы

### Вариант 1: "Супер-минимальный MVP" (1 месяц)
Убрать:
- ❌ AI анализ → только метрики
- ❌ Графики → только таблицы
- ❌ PostgreSQL → вообще без БД
- ✅ Только: Upload CSV → Get metrics JSON

**Плюс:** Быстро, дешево
**Минус:** Нет "WOW" эффекта, нет AI

### Вариант 2: "No-code прототип" (1 неделя)
- Bubble.io / Retool для UI
- n8n / Zapier для backend
- OpenAI API напрямую

**Плюс:** Супер-быстро для валидации
**Минус:** Не масштабируется, платная подписка

### Вариант 3: "Текущий MVP" (2.5-3 месяца) ✅
**Рекомендую этот!**

---

**Дата:** 2026-01-25  
**Reviewer:** Senior Fullstack Developer  
**Статус:** ✅ Ready to proceed with conditions
