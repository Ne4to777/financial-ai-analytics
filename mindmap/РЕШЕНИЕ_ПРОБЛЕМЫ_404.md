# 🔧 Решение проблемы 404 с Landing Page

## Что было исправлено

### Проблема
При клике на кнопку "Landing Page" в viewer'е появлялась ошибка 404.

### Причина
Кнопки в HTML использовали относительные пути (`../prototypes/01-landing.html`), которые работают по-разному в зависимости от того, как открыт viewer (через `file://` или `http://`).

### Решение
Обновлен код в `viewer/view-diagram-full.html`:
- Теперь автоматически определяется протокол (file:// или http://)
- Для HTTP используются **абсолютные пути** от корня сервера
- Для file:// используются относительные пути

**Код до:**
```javascript
const baseUrl = '../prototypes/index.html';
// Кнопка: href="${baseUrl.replace('index.html', '01-landing.html')}"
```

**Код после:**
```javascript
if (window.location.protocol === 'file:') {
    // Относительные пути для file://
    prototypeUrls.landing = '../prototypes/01-landing.html';
} else {
    // Абсолютные пути для http://
    prototypeUrls.landing = origin + '/prototypes/01-landing.html';
}
```

---

## ✅ Как использовать (правильно)

### Запустите локальный сервер:

```bash
cd /Users/nybble/projects/ident/mindmap
./start-server.sh
```

Или вручную:
```bash
npx live-server --port=8001 --open=/viewer/view-diagram-full.html
```

### Проверьте что открылся правильный URL:
```
http://localhost:8001/viewer/view-diagram-full.html
```

**Важно:** Должно быть `http://localhost:8001`, а НЕ `file:///`

### Откройте прототипы:
1. Выберите "🎨 HTML Прототипы (открыть)" в левом меню
2. ИЛИ выберите "6. Mind Map экранов" → кликните "🎨 HTML Прототипы"

### Кликните на кнопки:
- 🏠 Landing Page → откроется `http://localhost:8001/prototypes/01-landing.html`
- 📤 Upload Screen → откроется `http://localhost:8001/prototypes/02-upload.html`
- 📊 Analysis Results → откроется `http://localhost:8001/prototypes/03-analysis.html`

---

## 🧪 Тестирование

Используйте файл [TEST_PROTOTYPES.md](TEST_PROTOTYPES.md) для полного тестирования всех функций.

---

## 🐛 Если все еще не работает

### 1. Проверьте консоль браузера (F12)

Откройте консоль и посмотрите на ошибки. Ищите:
- URL который пытается открыть
- Ошибки 404 или CORS

### 2. Проверьте путь в "💡 Путь к прототипам"

В странице "prototypes" в viewer'е должен отображаться путь:
```
http://localhost:8001/prototypes/01-landing.html
```

Если видите `file:///...` - значит viewer открыт неправильно.

### 3. Вручную откройте прототип

Скопируйте и вставьте в браузер:
```
http://localhost:8001/prototypes/01-landing.html
```

Если работает - значит проблема в кнопках. Если не работает - проблема с сервером.

### 4. Проверьте что сервер запущен

```bash
# Проверьте что порт 8001 слушает
lsof -i :8001

# Или проверьте curl'ом
curl http://localhost:8001/
```

### 5. Перезапустите сервер

```bash
# Остановите (Ctrl+C или)
pkill -f "live-server.*8001"

# Запустите снова
cd /Users/nybble/projects/ident/mindmap
./start-server.sh
```

---

## 📁 Структура файлов

Убедитесь что все файлы на месте:

```bash
cd /Users/nybble/projects/ident/mindmap
ls -lh prototypes/*.html
```

Должно быть:
```
prototypes/index.html
prototypes/01-landing.html
prototypes/02-upload.html
prototypes/03-analysis.html
```

---

## ✅ Дополнительные улучшения

### 1. Создан скрипт `start-server.sh`
Автоматически запускает сервер и открывает viewer.

### 2. Обновлена документация
- README.md - главная инструкция
- БЫСТРЫЙ_СТАРТ.md - как запустить
- ОТКРЫТЬ_ПРОТОТИПЫ.md - все способы открытия
- TEST_PROTOTYPES.md - тестирование

### 3. Улучшена страница "prototypes" в viewer'е
Теперь показывает:
- Кнопки для каждого прототипа
- Путь к файлу (для отладки)
- Инструкции по запуску сервера

---

## 📞 Если проблема сохраняется

Проверьте:
1. ✅ Сервер запущен на порту 8001
2. ✅ Viewer открыт через `http://localhost:8001` (НЕ file://)
3. ✅ Все файлы прототипов существуют
4. ✅ Нет ошибок в консоли браузера (F12)

Если все выше проверено и не работает - опишите:
- Какой URL показывается в адресной строке
- Какие ошибки в консоли (F12)
- Скриншот страницы "prototypes" в viewer'е

---

**Дата исправления:** 2024-01-25  
**Статус:** ✅ Исправлено  
**Версия:** 1.1
