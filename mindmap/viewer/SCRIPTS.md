# Скрипты для работы с диаграммами

## 🚀 Управление сервером

### Запуск dev сервера
```bash
./start.sh
# или через npm
npm start
```
Запускает HTTP сервер на порту 8000 и WebSocket на 8001 с hot-reload.

### Остановка dev сервера
```bash
./stop-server.sh
# или через npm
npm run stop
```
Принудительно останавливает все процессы на портах 8000 и 8001.

### Перезапуск dev сервера
```bash
npm run restart
```
Останавливает и сразу запускает сервер заново.

**Когда использовать:**
- ❌ Закрыли терминал, но сервер остался запущенным
- ❌ Получили ошибку "address already in use"
- ❌ Нужно перезапустить сервер

## 📸 Скриншоты диаграмм

### Сделать скриншот одной диаграммы
```bash
node screenshot-diagram.js 01
# или через npm
npm run screenshot 01

node screenshot-diagram.js 02
npm run screenshot 02
# и т.д.
```

Скриншоты сохраняются в `screenshots/diagram-XX.png`

### Сделать скриншоты всех диаграмм
```bash
for i in {01..11}; do
    node screenshot-diagram.js $i
    sleep 1
done
```

## 🔄 Обновление главного mindmap

```bash
./update-main-mindmap.sh
```

Собирает все диаграммы из `diagrams/` в единый файл `PROJECT_MINDMAP.md`.

**Когда использовать:**
- После изменения любой диаграммы в `diagrams/`
- Перед коммитом изменений

## 📋 Экспорт диаграмм в SVG

```bash
./export-diagrams.sh
```

Экспортирует все диаграммы в формат SVG в папку `exported-diagrams/`.

## 🚨 Устранение проблем

### Порт занят
```bash
# Проверить что занимает порт
lsof -i :8000
lsof -i :8001

# Остановить всё
./stop-server.sh

# Запустить снова
./start.sh
```

### Не загружаются диаграммы
```bash
# 1. Остановить сервер
./stop-server.sh

# 2. Проверить что файлы на месте
ls -la ../diagrams/*.md

# 3. Обновить главный mindmap
./update-main-mindmap.sh

# 4. Запустить снова
./start.sh
```

### Скриншоты не работают
```bash
# Проверить установлен ли Playwright
npm list playwright

# Переустановить если нужно
npm install --save-dev playwright
npx playwright install chromium
```
