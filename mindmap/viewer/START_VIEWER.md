# 🚀 Как запустить Diagram Viewer

## Проблема

`view-diagram-full.html` загружает диаграммы из файлов `.md`, но браузеры блокируют локальные файлы из-за CORS политики.

## ✅ Решение: Запустите локальный веб-сервер

### Вариант 1: Python (проще всего)

```bash
# Если у вас Python 3:
python3 -m http.server 8000

# Или Python 2:
python -m SimpleHTTPServer 8000
```

Затем откройте: **http://localhost:8000/view-diagram-full.html**

---

### Вариант 2: Node.js (http-server)

```bash
# Установите http-server глобально
npm install -g http-server

# Запустите из корня проекта
http-server -p 8000
```

Затем откройте: **http://localhost:8000/view-diagram-full.html**

---

### Вариант 3: PHP

```bash
php -S localhost:8000
```

Затем откройте: **http://localhost:8000/view-diagram-full.html**

---

### Вариант 4: VS Code Live Server

1. Установите расширение "Live Server"
2. Правый клик на `view-diagram-full.html`
3. Выберите "Open with Live Server"

---

## 🎯 Быстрый старт (рекомендуется)

### С автоматическим скриптом и hot-reload:

```bash
cd mindmap/viewer

# Запустить dev сервер с hot-reload
./start.sh

# Откроется браузер автоматически
```

### Или вручную:

```bash
# Из корня проекта /Users/nybble/projects/ident

# Запустите сервер (выберите любой способ выше)
python3 -m http.server 8000

# Откройте в браузере
open http://localhost:8000/mindmap/viewer/view-diagram-full.html
```

Теперь все 11 диаграмм будут загружаться автоматически! 🎨

---

## 🛑 Остановка сервера

```bash
cd mindmap/viewer

# Через npm (рекомендуется)
npm run stop

# Или напрямую через скрипт
./stop-server.sh
```

**Используйте когда:**
- Закрыли терминал, но сервер остался запущенным
- Получили ошибку "address already in use"  
- Нужно перезапустить сервер

### Перезапуск сервера

```bash
npm run restart  # Остановит и запустит заново
```

---

## 🔍 Альтернатива: Mermaid Live Editor

Если не хочется запускать сервер:

1. Откройте https://mermaid.live
2. Откройте любой файл из `docs/diagrams/`
3. Скопируйте код между \`\`\`mermaid и \`\`\`
4. Вставьте в Mermaid Live Editor
5. Полный зум и экспорт работают из коробки! ✨

---

## ❓ Troubleshooting

### Ошибка: "Failed to load resource"
→ Вы открыли HTML напрямую из файла (file://). Используйте локальный сервер!

### Ошибка: "Address already in use"
→ Порт 8000 занят. Используйте другой: `python3 -m http.server 8001`

### Диаграммы не загружаются
→ Убедитесь что вы в корне проекта (`/Users/nybble/projects/ident`)
