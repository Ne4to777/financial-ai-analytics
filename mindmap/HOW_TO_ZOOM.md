# 🔍 Как просматривать диаграммы с зумом

## Лучшие способы с поддержкой зума

### 🥇 Способ 1: Mermaid Live Editor (Рекомендуется)

**Самый удобный способ с полным зумом и панорамированием!**

1. Откройте [Mermaid Live Editor](https://mermaid.live)
2. Скопируйте код диаграммы из файла (только блок между \`\`\`mermaid и \`\`\`)
3. Вставьте в левую панель редактора
4. Справа увидите отрендеренную диаграмму

**Управление:**
- 🖱️ **Колесико мыши** - зум in/out
- ⌨️ **Cmd/Ctrl + колесико** - более плавный зум
- 🖐️ **Drag & Drop** - перемещение по диаграмме
- 🔽 **Кнопка "Actions"** → **PNG/SVG** - экспорт в высоком разрешении

**Скрипт для быстрого копирования:**
```bash
# Копирует код диаграммы в буфер обмена (macOS)
cat docs/diagrams/01-project-structure.md | sed -n '/```mermaid/,/```/p' | pbcopy
# Теперь просто вставьте в Mermaid Live Editor
```

---

### 🥈 Способ 2: GitHub (Автоматический рендеринг)

1. Загрузите проект на GitHub:
```bash
git init
git add .
git commit -m "Add diagrams"
git remote add origin https://github.com/your-username/repo.git
git push -u origin main
```

2. Откройте любой файл диаграммы на GitHub
3. GitHub отрендерит SVG автоматически
4. **Для зума:**
   - 🖱️ **Правый клик** на диаграмме → **Open image in new tab**
   - В новой вкладке используйте **Cmd/Ctrl + Plus/Minus** для зума
   - Или используйте встроенный зум браузера

---

### 🥉 Способ 3: VS Code с расширением (Локально)

#### Установка расширений:

1. **Markdown Preview Mermaid Support**
   - Extensions → Поиск → "Markdown Preview Mermaid Support"
   - Install

2. **Markdown Preview Enhanced** (более мощный вариант)
   - Extensions → Поиск → "Markdown Preview Enhanced"
   - Install
   - Поддерживает экспорт в PDF, PNG, SVG

#### Использование:
```
1. Откройте файл диаграммы
2. Cmd+Shift+V (Mac) / Ctrl+Shift+V (Windows) - Preview
3. Зум в preview:
   - Cmd/Ctrl + Plus/Minus
   - Или через меню View → Zoom In/Out
```

#### Экспорт из VS Code:
```
1. Правый клик в preview панели
2. "Markdown Preview Enhanced: Open in Browser"
3. В браузере: Правый клик → Save as → SVG/PNG
4. Открываем SVG в любом векторном редакторе (бесконечный зум!)
```

---

### 🎨 Способ 4: Экспорт в SVG/PNG (Высокое разрешение)

#### Через Mermaid CLI (для автоматизации):

```bash
# Установка
npm install -g @mermaid-js/mermaid-cli

# Экспорт одной диаграммы в SVG
mmdc -i docs/diagrams/01-project-structure.md -o output/project-structure.svg

# Экспорт в PNG с высоким разрешением
mmdc -i docs/diagrams/01-project-structure.md -o output/project-structure.png -w 4000 -H 3000

# Экспорт всех диаграмм
for file in docs/diagrams/*.md; do
  filename=$(basename "$file" .md)
  mmdc -i "$file" -o "output/${filename}.svg"
done
```

#### Просмотр SVG с зумом:
- **macOS:** Открыть в Safari, Preview, или любом векторном редакторе
- **Windows:** Открыть в Edge, Chrome, или Inkscape
- **Linux:** Открыть в Firefox, Chrome, или Inkscape

SVG = векторный формат = **бесконечный зум без потери качества!** 🎉

---

### 🖥️ Способ 5: Браузер с локальным файлом

#### Создайте HTML файл для просмотра:

```bash
# Создайте файл viewer.html
cat > viewer.html << 'EOF'
<!DOCTYPE html>
<html>
<head>
    <script src="https://cdn.jsdelivr.net/npm/mermaid/dist/mermaid.min.js"></script>
    <style>
        body { 
            margin: 20px; 
            background: #f5f5f5;
        }
        .mermaid {
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
    </style>
</head>
<body>
    <div class="mermaid">
        <!-- Вставьте сюда код диаграммы -->
    </div>
    <script>
        mermaid.initialize({ 
            startOnLoad: true,
            theme: 'default',
            flowchart: { useMaxWidth: false }
        });
    </script>
</body>
</html>
EOF
```

Откройте `viewer.html` в браузере и используйте **Cmd/Ctrl + Plus/Minus** для зума!

---

### 📱 Способ 6: Obsidian (Для продвинутых)

Если вы используете Obsidian для управления знаниями:

1. Откройте папку проекта как Vault
2. Obsidian нативно рендерит Mermaid
3. **Zoom in Canvas mode:**
   - Откройте диаграмму
   - Cmd/Ctrl + клик → "Open in Canvas"
   - В Canvas используйте **колесико мыши** для зума
   - Полная интерактивность!

---

## 🚀 Быстрые команды

### Скрипт для автоматического экспорта всех диаграмм в SVG:

```bash
#!/bin/bash
# save as: export-diagrams.sh

# Создаем папку для экспорта
mkdir -p exported-diagrams

# Устанавливаем mermaid-cli если нет
if ! command -v mmdc &> /dev/null; then
    echo "Installing mermaid-cli..."
    npm install -g @mermaid-js/mermaid-cli
fi

# Экспортируем все диаграммы
for file in docs/diagrams/*.md; do
    if [[ $file != *"README.md" ]]; then
        filename=$(basename "$file" .md)
        echo "Exporting $filename..."
        mmdc -i "$file" -o "exported-diagrams/${filename}.svg" -b transparent
    fi
done

echo "✅ All diagrams exported to exported-diagrams/"
echo "Open SVG files in browser for infinite zoom!"
```

Использование:
```bash
chmod +x export-diagrams.sh
./export-diagrams.sh
```

---

## 📊 Сравнение способов

| Способ | Зум | Качество | Простота | Оффлайн |
|--------|-----|----------|----------|---------|
| Mermaid Live | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ❌ |
| GitHub | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ❌ |
| VS Code | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ |
| SVG Export | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ✅ |
| HTML Viewer | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ✅ |
| Obsidian | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ✅ |

---

## 💡 Рекомендации

**Для быстрого просмотра:**
→ Используйте **Mermaid Live Editor** (копируй-вставь-зум)

**Для презентаций:**
→ Экспортируйте в **SVG** и открывайте в браузере

**Для работы оффлайн:**
→ Используйте **VS Code** с расширениями

**Для редактирования:**
→ Работайте в **Mermaid Live Editor**, копируйте обратно в файл

---

## 🎯 Мой рекомендуемый workflow:

1. **Разработка:** Пишу код в VS Code
2. **Просмотр:** Копирую в Mermaid Live Editor для детального просмотра
3. **Презентация:** Экспортирую в SVG для показа на большом экране
4. **Документация:** Коммичу в GitHub для автоматического рендеринга

---

## 🐛 Troubleshooting

**Диаграмма не помещается на экран?**
→ Экспортируйте в SVG и откройте в браузере

**Текст слишком мелкий?**
→ В Mermaid Live Editor используйте зум или экспортируйте в большем разрешении

**Нужно показать диаграмму коллегам?**
→ Создайте публичный gist на GitHub или поделитесь ссылкой на Mermaid Live

**Хочу редактировать и сразу видеть результат?**
→ Используйте Mermaid Live Editor в split-screen режиме!

---

Наслаждайтесь красивыми диаграммами с идеальным зумом! 🎨🔍
