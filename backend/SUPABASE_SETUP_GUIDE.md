# 🗄️ Supabase Setup Guide - Пошаговая Инструкция

## Шаг 1: Создание проекта Supabase

### 1.1 Откройте Dashboard
Перейдите на: https://supabase.com/dashboard

### 1.2 Создайте новый проект
1. Нажмите **"New Project"** (зеленая кнопка справа вверху)
2. Если появится выбор организации - выберите вашу (обычно ваш username)

### 1.3 Заполните форму
```
Name: csv-processing-api
   ↑ Любое имя для проекта

Database Password: [придумайте сильный пароль]
   ↑ ОБЯЗАТЕЛЬНО СОХРАНИТЕ! Понадобится для прямого доступа к БД

Region: Central EU (Frankfurt)
   ↑ Выберите ближайший регион

Pricing Plan: Free
   ↑ Бесплатный план (500MB database, достаточно для старта)
```

### 1.4 Создайте проект
- Нажмите **"Create new project"**
- Подождите 2-3 минуты (будет индикатор загрузки)
- ✅ Проект готов когда индикатор исчезнет

---

## Шаг 2: Получение API credentials

### 2.1 Откройте настройки API
1. В левом боковом меню найдите иконку **⚙️ Settings** (самая нижняя)
2. Нажмите на **Settings**
3. В открывшемся подменю выберите **"API"**

### 2.2 Найдите Project API keys
Прокрутите вниз до раздела **"Project API keys"**

Вы увидите три поля:

#### A) Project URL
```
Project URL: https://xxxxxxxxxxxxx.supabase.co
                    ↑↑↑↑↑↑↑↑↑↑↑↑↑
                    Ваш уникальный project ID
```
**📋 СКОПИРУЙТЕ весь URL!**

#### B) API Keys
```
anon public:
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZ...
↑↑↑↑↑↑↑
Очень длинный ключ (примерно 200+ символов)
```
**📋 СКОПИРУЙТЕ весь ключ!** (нажмите на иконку 📋 справа от ключа)

⚠️ **НЕ КОПИРУЙТЕ `service_role` key!** - он для админских операций

---

## Шаг 3: Создание .env файла

### Вариант A: Автоматически (рекомендуется)

Откройте терминал и выполните:

```bash
cd /Users/nybble/projects/ident/backend
./setup-supabase.sh
```

Скрипт попросит вас ввести URL и ключ, и создаст `.env` автоматически.

### Вариант B: Вручную

Создайте файл `.env` в папке `backend/`:

```bash
cd /Users/nybble/projects/ident/backend

# Создайте файл
cat > .env << 'EOF'
# Supabase Configuration
SUPABASE_URL=https://ваш-project-id-здесь.supabase.co
SUPABASE_ANON_KEY=ваш-очень-длинный-ключ-здесь

# Server Configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# Logging
LOG_LEVEL=info
EOF
```

**Замените:**
- `https://ваш-project-id-здесь.supabase.co` → на ваш реальный URL
- `ваш-очень-длинный-ключ-здесь` → на ваш реальный anon key

### Проверка
```bash
# Проверьте, что файл создан
cat .env

# Должны увидеть:
# SUPABASE_URL=https://xxxxx.supabase.co
# SUPABASE_ANON_KEY=eyJh...
```

⚠️ **ВАЖНО:** Файл `.env` уже в `.gitignore`, не комитьте его в Git!

---

## Шаг 4: Запуск миграций базы данных

### 4.1 Откройте SQL Editor
1. В Supabase Dashboard, в левом меню выберите **"SQL Editor"**
2. Нажмите **"New query"** (или кнопку **"+"**)

### 4.2 Запустите Миграцию 1: Таблица uploads

Скопируйте **весь** файл:
```
/Users/nybble/projects/ident/backend/database/migrations/001_create_uploads_table.sql
```

1. Откройте файл в редакторе
2. Скопируйте все содержимое (Cmd+A, Cmd+C)
3. Вставьте в SQL Editor в Supabase
4. Нажмите **"Run"** (или Cmd+Enter)
5. ✅ Должно появиться: "Success. No rows returned"

### 4.3 Запустите Миграцию 2: Таблица transactions

Создайте новый query (**"New query"** или **"+"**)

Скопируйте **весь** файл:
```
/Users/nybble/projects/ident/backend/database/migrations/002_create_transactions_table.sql
```

1. Скопируйте все содержимое
2. Вставьте в SQL Editor
3. Нажмите **"Run"**
4. ✅ Должно появиться: "Success. No rows returned"

### 4.4 Проверка таблиц

Создайте новый query и выполните:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('uploads', 'transactions');
```

**Ожидаемый результат:**
```
table_name
------------
uploads
transactions
```

✅ Если видите обе таблицы - БД готова!

---

## Шаг 5: Тестирование подключения (опционально)

```bash
cd /Users/nybble/projects/ident/backend

# Установите зависимости если еще не установлены
npm install

# Запустите тест подключения
npm run test:connection
```

✅ Должно появиться: "Connection successful!"

---

## 🎯 Готово!

Теперь у вас:
- ✅ Создан проект Supabase
- ✅ Получены API credentials
- ✅ Создан файл `.env`
- ✅ Запущены миграции БД
- ✅ Таблицы `uploads` и `transactions` созданы

**Следующий шаг:** Деплой на Vercel!

```bash
cd /Users/nybble/projects/ident/backend
./deploy-vercel.sh
```

---

## 🆘 Troubleshooting

### Проблема: "Invalid API key"
- Убедитесь, что скопировали **anon public**, а не service_role
- Проверьте, что ключ не обрезан (должен быть ~200+ символов)

### Проблема: "Project not found"
- Проверьте URL - должен быть `https://xxxxx.supabase.co`
- Убедитесь, что проект полностью создан (прошло 2-3 минуты)

### Проблема: "Table already exists"
- Это нормально, если запускаете миграцию повторно
- Можно пропустить или удалить таблицы и создать заново:
  ```sql
  DROP TABLE IF EXISTS transactions CASCADE;
  DROP TABLE IF EXISTS uploads CASCADE;
  ```

### Проблема: "Connection failed"
- Проверьте `.env` файл (без опечаток)
- Убедитесь, что нет лишних пробелов
- Проверьте, что URL начинается с `https://`

---

## 📞 Где найти что

| Что нужно | Где найти |
|-----------|-----------|
| Project URL | Settings → API → Project URL |
| anon key | Settings → API → Project API keys → anon public |
| Database password | Вы создали при создании проекта (если забыли - можно сбросить в Settings → Database) |
| SQL Editor | Левое меню → SQL Editor |
| Таблицы | Table Editor → выберите таблицу |
| Логи | Logs → выберите тип логов |

---

**Дата:** 27 января 2026  
**Версия:** 1.0.0
